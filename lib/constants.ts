// Constantes compartidas de la app.

// Clave de localStorage donde se persiste el código de invitación a través de
// todo el flujo de nuevo usuario (unirse → onboarding → tour → unirse al grupo).
export const PENDING_INVITE_KEY = "olympo_pending_invite";
